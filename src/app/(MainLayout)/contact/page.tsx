import NoDesign from "@/components/UI/NoDesign";
import { metaDataSEO } from "@/utils/MetaData";
export const metadata = metaDataSEO({
  tabTitle: "roomBooker | Contact",
  des: "Welcome to our room booking website.Contact page  ",
});
const page = () => {
  return (
    <div>
      <NoDesign />
    </div>
  );
};

export default page;
